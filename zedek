<?php

$app = $_SERVER['argv'][0];
$port = isset($_SERVER['argv'][1]) ? $_SERVER['argv'][1] : false;

$io = 
	$port == false ? 
		shell_exec("php -S localhost:8585 -t ./public ./public/zedek") : 
		shell_exec("php -S localhost:{$port} -t ./public ./public/zedek");