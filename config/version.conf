{
	"version":"4",
	"sub_version":"4.3.0.22072015", 
	"change_log":"added methods to internals. _Form::posted() to check if the form was posted and unsets submit key. _Auth now access ZORM instead of _ORM and view read app in ::template from global. angular.min.js included in common"
}
